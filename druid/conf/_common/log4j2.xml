<?xml version="1.0" encoding="UTF-8"?>
<!--
  Zomato Data Platform - Apache Druid Log4j2 Configuration
  Shared logging config across all Druid services.
  Logs are collected by Fluentd and shipped to CloudWatch Logs.
-->
<Configuration status="WARN">
  <Properties>
    <Property name="log.dir">/var/log/druid</Property>
    <Property name="log.pattern">%d{ISO8601} %p [%t] %c - %m%n</Property>
    <Property name="json.pattern">{"timestamp":"%d{ISO8601}","level":"%p","thread":"%t","logger":"%c","message":"%m","exception":"%xEx"}%n</Property>
  </Properties>

  <Appenders>
    <!-- Console appender for container stdout -->
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="${log.pattern}"/>
    </Console>

    <!-- Rolling file appender for structured JSON logs -->
    <RollingFile name="RollingFile"
                 fileName="${log.dir}/${sys:druid.node.type}.log"
                 filePattern="${log.dir}/${sys:druid.node.type}-%d{yyyy-MM-dd}-%i.log.gz">
      <PatternLayout pattern="${json.pattern}"/>
      <Policies>
        <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
        <SizeBasedTriggeringPolicy size="500MB"/>
      </Policies>
      <DefaultRolloverStrategy max="14">
        <Delete basePath="${log.dir}" maxDepth="1">
          <IfFileName glob="${sys:druid.node.type}-*.log.gz"/>
          <IfLastModified age="14d"/>
        </Delete>
      </DefaultRolloverStrategy>
    </RollingFile>

    <!-- Separate file for audit logs -->
    <RollingFile name="AuditLog"
                 fileName="${log.dir}/audit.log"
                 filePattern="${log.dir}/audit-%d{yyyy-MM-dd}-%i.log.gz">
      <PatternLayout pattern="${json.pattern}"/>
      <Policies>
        <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
        <SizeBasedTriggeringPolicy size="200MB"/>
      </Policies>
      <DefaultRolloverStrategy max="30"/>
    </RollingFile>
  </Appenders>

  <Loggers>
    <!-- Druid core -->
    <Logger name="org.apache.druid" level="INFO" additivity="false">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="RollingFile"/>
    </Logger>

    <!-- Kafka indexing service - more verbose for ingestion debugging -->
    <Logger name="org.apache.druid.indexing.kafka" level="INFO" additivity="false">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="RollingFile"/>
    </Logger>

    <!-- SQL planning -->
    <Logger name="org.apache.druid.sql" level="INFO" additivity="false">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="RollingFile"/>
    </Logger>

    <!-- Audit logging -->
    <Logger name="org.apache.druid.server.security.AuthenticationUtils" level="INFO" additivity="false">
      <AppenderRef ref="AuditLog"/>
    </Logger>

    <!-- Reduce noise from chatty libraries -->
    <Logger name="org.apache.zookeeper" level="WARN"/>
    <Logger name="org.apache.curator" level="WARN"/>
    <Logger name="org.eclipse.jetty" level="WARN"/>
    <Logger name="com.amazonaws" level="WARN"/>
    <Logger name="org.apache.kafka" level="WARN"/>
    <Logger name="org.jboss.netty" level="WARN"/>

    <Root level="INFO">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="RollingFile"/>
    </Root>
  </Loggers>
</Configuration>
